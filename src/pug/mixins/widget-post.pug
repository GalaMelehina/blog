mixin widget-post(img, title, subtitle)
    .widget-post 
        .widget-post__img 
            img(src= img, alt="Photo")
        a(href="#").widget-post__title
            h5.title-5 #{title}
        .widget-post__subtitle 
            span #{subtitle}